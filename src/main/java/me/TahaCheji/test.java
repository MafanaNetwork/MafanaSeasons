package me.TahaCheji;

import me.TahaCheji.data.Season;
import org.bukkit.World;

import java.util.concurrent.ThreadLocalRandom;

public class test{
    /*
    public test() {
        super("Summer", 1);
    }

    int i = 0;

    @Override
    public boolean whileThere(World world) {
        System.out.println("Summer!!");
        return true;
    }

    boolean hasStarted = false;
    @Override
    public boolean eventOne(World world) {
        if(!hasStarted) {
            int r = ThreadLocalRandom.current().nextInt(1, 4);
            if (r == 1) {
                hasStarted = true;
                i += 1;
            }
        } else {
            i += 1;
            if(i >= 3) {
                System.out.println("Summer Event For 3 days");
            }
        }
        return true;
    }
     */
}
